define(['marionette', 'Views/Controls/toolboxItemView'],
    function(Marionette, ItemView) {
        var toolboxItemView = Backbone.Marionette.CompositeView.extend({
            initialize: function() {
                // initialize dynatree here
            }
        });
        return toolboxItemView;
    });
